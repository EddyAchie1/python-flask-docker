Flask==2.3.3
#Flask-Cors==3.1.2        # To handle Cross-Origin Resource Sharing (CORS) if needed
#Flask-RESTful==1.0.0     # For building REST APIs with Flask
requests==2.28.2        # For making HTTP requests, useful for API integrations
gunicorn==20.1.0        # WSGI HTTP server for running Flask in production